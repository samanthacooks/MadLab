<%= form_for @experiment do |f| %>

  <% if @experiment.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@experiment.errors.count, "error") %> prohibited
        this proposal from being saved:
      </h2>
      <ul>
        <% @experiment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <p>
    <%= f.label :category %><br>
    <%= f.text_area :category %>
  </p>

  <p>
    <%= f.label :results %><br>
    <%= f.text_area :results %>
  </p>

  <p>
    <%= f.label :conclusions %><br>
    <%= f.text_area :conclusions %>
  </p>


  <p class="submit">
    <%= f.submit %>
  </p>

<% end %>

